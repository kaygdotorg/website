/**
 * Shared image optimization settings.
 * Used by astro.config.mjs and image-manifest.json.ts for consistency.
 */

/** Quality setting for WebP/AVIF output (0-100, lower = smaller files) */
export const IMAGE_QUALITY = 70;
